.col-xs-12.col-sm-6.col-md-6.col-lg-6(ng-show='collection')
  h4 Keys - Add field names for the current model
  .input-group
    input.form-control(
      ng-keypress="($event.which === 13)?addKey():0",
      type='text', ng-model='name', required='')
    span.input-group-btn
      a.btn.btn-success.fa.fa-plus(ng-click='addKey()', ng-disabled='!collection')
  .pre-scrollable
    table.table
      thead
        tr
          th
          th Name
          th Tag Element
          th Actions
      tbody
        tr(ng-repeat='field in resource.schema')
          td
            input(type='checkbox', ng-model='field.active')
          td
            span(ng-bind='field.name')
          td
            select.form-control(ng-model='field.tag')
              option input
              option select
              option textarea
          td
            a.btn.btn-primary.fa.fa-shield(
              ng-show='field.exclude',
              ng-click='excludeToggle(field.name, field.exclude )')
            a.btn.btn-warning.fa.fa-users(
              ng-show='!field.exclude',
              ng-click='excludeToggle(field.name, field.exclude )')
            a.btn.btn-danger.fa.fa-trash(ng-click='removeKey(field.name)')
            a.btn.btn-success(ng-click='pinKey(field.name)')
              span.glyphicon.glyphicon-pushpin
            
